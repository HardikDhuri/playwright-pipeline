{
  "name": "playwright-saucedemo-poc",
  "version": "1.0.0",
  "description": "Parallel Playwright tests for SauceDemo on GitHub Actions",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
